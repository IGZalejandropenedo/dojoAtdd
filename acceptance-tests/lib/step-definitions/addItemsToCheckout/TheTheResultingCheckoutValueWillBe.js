'use strict';

module.exports = function() {

  this.Then(/^the resulting checkout value will be "([^"]*)"$/, function (amount, callback) {
    // Write code here that turns the phrase above into concrete actions
    callback(null, 'pending');
  });

};
